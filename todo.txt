) Move utilities into own folder
) Change the CLI to be a wrapper around an API core
) init script to create a tspackage.json file (ala npm init)
) Some sort of repository support
) Split out tspackage.json facilities into own module so that it can be easily
    consumed from typescript module core
